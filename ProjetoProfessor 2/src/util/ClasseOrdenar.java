/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package util;

import classeDados.Professor;
import java.util.ArrayList;


/**
 *
 * @author leticiasilva
 */
public abstract class ClasseOrdenar {
    

    /*public ArrayList bolha(ArrayList<Professor> array) throws Exception {
        
        
        

        //ArrayList<Professor> array = new ArrayList<Professor>();
        for (int i = 0; i < array.size(); i++) {
            for (int j = i; j < array.size(); j++) {

                if (!compara(array.get(i), array.get(j))) {
                    Professor temp = array.get(j);
                    array.set(j, array.get(i));
                    array.set(i, temp);
                }
            }
        }

        return array;
    }

    public abstract boolean compara(Professor professor1, Professor professor2);
*/
}
